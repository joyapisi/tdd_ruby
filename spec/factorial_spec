describe Factorial do
  it('should return the factorial of a given number') do
    calc = Factorial.new
    expect(calc.find(3)).to eq(6)
  end
  it('should return 1 for a factorial of 0') do
    calc = Factorial.new
    expect(calc.find(0)).to eq(1)
  end
end